export type ValidateCodeAuthDto = { code: string };

/**
 * @swagger
 * components:
 *  schemas:
 *    ValidateCodeAuthDto:
 *      description: Тело запроса /api/auth/validate/code
 *      type: object
 *      properties:
 *        code:
 *          type: string
 */
